export interface Vserver {
  _id: string,
  numId: number,
  user_id: string,
  tariff_id: string,
  user: string,
  ctid: number,
  ip: string,
  username: string,
  password: string,
  os: string,
  price: number,
  ending_date: Date,
  available: boolean
}
